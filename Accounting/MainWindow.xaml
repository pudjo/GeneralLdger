<r:RibbonWindow x:Class="Accounting.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        
        xmlns:local="clr-namespace:Accounting"
        xmlns:views="clr-namespace:Accounting.Views"
        xmlns:r="clr-namespace:System.Windows.Controls.Ribbon;assembly=System.Windows.Controls.Ribbon"
        xmlns:vms="clr-namespace:Accounting"
               mc:Ignorable="d"         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}"
              Title="Accounting" Height="450" Width="800" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height ="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- RIBBON di baris atas -->
        <r:Ribbon x:Name="MainRibbon" Grid.Row="0" Title="Accounting" Height="120" Background="{DynamicResource MaterialDesignPaper}">
            <!-- Static test tab to verify Ribbon and PackIcon render correctly -->
            <r:Ribbon.Resources>
                <!-- Memaksa semua teks Tab Header Ribbon menjadi Bold -->
                <Style TargetType="r:RibbonTab">
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>

                <!-- Memaksa semua teks Group Header Ribbon menjadi Bold -->
                <Style TargetType="r:RibbonGroup">
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </r:Ribbon.Resources>
            
            
            <!-- Tabs akan dibuat dari code-behind -->
        </r:Ribbon>

        <!-- Konten aplikasi tetap di bawah ribbon -->
        <materialDesign:DrawerHost Grid.Row="1" IsLeftDrawerOpen="{Binding IsMenuOpen}">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="220">
                    <Grid HorizontalAlignment="Stretch" Grid.Row="1" Background="BurlyWood">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"/>
                            <RowDefinition Height="326*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Background="GhostWhite">
                            <Image Source="pack://application:,,,/Assets/logo.png"/>
                        </Grid>
                        <ScrollViewer HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Grid.Row="1">
                            <StackPanel x:Name="Menu" Margin="8"/>
                        </ScrollViewer>
                    </Grid>
                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>

            <DockPanel>
                <ContentControl Content="{Binding SelectedItem}" x:Name="MainContentArea" Margin="16"/>
            </DockPanel>
        </materialDesign:DrawerHost>
    </Grid>
    </r:RibbonWindow>