<UserControl x:Class="Accounting.Views.AR.CustomerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting.Views.AR"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search panel -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,0,0,8" >
            <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                <TextBlock Text="Code" FontWeight="Bold"/>
                <TextBox Width="180" Text="{Binding CodeFilter, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                <TextBlock Text="Name" FontWeight="Bold"/>
                <TextBox Width="260" Text="{Binding NameFilter, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Orientation="Vertical" VerticalAlignment="Bottom" Margin="8,0,0,0">
                <Button Content="Search" Width="90" Command="{Binding SearchCommand}" />
            </StackPanel>

            <StackPanel Orientation="Vertical" VerticalAlignment="Bottom" Margin="8,0,0,0">
                <Button Content="Add" Width="90" Command="{Binding AddCommand}" />
            </StackPanel>
        </StackPanel>

        <!-- spacer -->
        <Border Grid.Row="1" />

        <!-- Results grid -->
        <DataGrid Style="{StaticResource DataGridStandar}" Grid.Row="2"
                  ItemsSource="{Binding Results}"
                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Visibility="Hidden" />
                <DataGridTemplateColumn Header="Detail" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Detail"
                                    Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}" Width="70" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250" />
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="300" />
                <DataGridTextColumn Header="Mobile" Binding="{Binding Phone}" Width="140" />
                <DataGridTextColumn Header="Telephone" Binding="{Binding ContactPerson}" Width="140" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
