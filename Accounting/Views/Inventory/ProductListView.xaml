<UserControl x:Class="Accounting.Views.Inventory.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting.Views.Inventory"
            xmlns:conv="clr-namespace:Accounting.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
    </UserControl.Resources>


    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search panel -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,0,0,8">
            <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                <TextBlock Text="Search" FontWeight="Bold"/>
                <TextBox Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <Button Content="Search" Width="90" Command="{Binding SearchCommand}" Margin="8,18,0,0"/>
            <Button Content="Add" Width="90" Command="{Binding AddCommand}" Margin="8,18,0,0"/>
            <Button Content="Refresh" Width="90" Command="{Binding RefreshCommand}" Margin="8,18,0,0"/>
        </StackPanel>


        <!-- Products grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Products}"
                  SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Height="420">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Visibility="Hidden" />

                <!-- Pilih column: button visible only when IsSelectionMode == true -->
                <!-- Action column: 'Pilih' visible when in selection mode.
                     'Detail' and 'Hapus' visible when NOT selection mode. -->
                <DataGridTemplateColumn Header="Action" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Pilih"
                                        Width="56"
                                        Margin="0,0,4,0"
                                        Visibility="{Binding DataContext.IsSelectionMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}"
                                        Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />

                                <Button Content="Detail"
                                        Width="56"
                                        Margin="0,0,4,0"
                                        Visibility="{Binding DataContext.IsSelectionMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBoolToVis}}"
                                        Command="{Binding DataContext.DetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />

                                <Button Content="Hapus"
                                        Width="56"
                                        Visibility="{Binding DataContext.IsSelectionMode, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseBoolToVis}}"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="150"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="300"/>
                <DataGridTextColumn Header="Purchase" Binding="{Binding PurchasePrice, StringFormat=N2}" Width="120"/>
                <DataGridTextColumn Header="Selling" Binding="{Binding CurrentSellingPrice, StringFormat=N2}" Width="120"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding CurrentStock}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- footer -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="90" Command="{Binding CloseCommand}" Margin="0,0,8,0"/>
        </StackPanel>
    </Grid>

</UserControl>
