<UserControl x:Class="Accounting.Views.Inventory.ProductPriceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting.Views.Inventory"
                          mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <Button Content="Refresh" Width="100" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Product.Id}" Width="60"/>
                <DataGridTextColumn Header="Kode" Binding="{Binding Product.Code}" Width="140"/>
                <DataGridTextColumn Header="Nama Barang" Binding="{Binding Product.Name}" Width="*"/>
                <!-- Editable Harga Jual -->
                <DataGridTemplateColumn Header="Harga Jual" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding EditablePrice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Save button per row -->
                <DataGridTemplateColumn Header="Aksi" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Simpan" Padding="6,2"
                                    Command="{Binding SaveCommand}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</UserControl>
