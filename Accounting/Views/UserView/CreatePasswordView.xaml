<Window x:Class="Accounting.Views.UserView.CreatePasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Accounting.Views.UserView"
        mc:Ignorable="d"
        Title="CreatePasswordView" Height="450" Width="600">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Settihg Password Pengguna" FontSize="20" FontWeight="Bold" Foreground="#333333" Margin="0,0,0,20"/>

        <!-- Form Input -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top">

            <!-- User ID (ReadOnly) -->
            <TextBlock Text="User ID" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding SelectedUser.UserID}" FontSize="14" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

            <!-- Nama (ReadOnly) -->
            <TextBlock Text="Nama Lengkap" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding SelectedUser.Name}" FontSize="14" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,15"/>

            <!-- Password Baru -->
            <TextBlock Text="Password Baru" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

            <!-- Confirm Password -->
            <TextBlock Text="Konfirmasi Password Baru" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" VerticalContentAlignment="Center">
                <!-- Logika trigger warna border merah jika tidak cocok -->
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="BorderBrush" Value="#CCCCCC"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsPasswordMatch}" Value="False">
                                <Setter Property="BorderBrush" Value="Red"/>
                                <Setter Property="BorderThickness" Value="1.5"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </StackPanel>

        <!-- Tombol Aksi -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Simpan Password" 
                    Command="{Binding SimpanPasswordCommand}"
                    Width="140" Height="35" FontWeight="Bold"
                    Background="#448AFF" Foreground="White">
                <!-- Style untuk membuat tombol terlihat abu-abu saat disabled -->
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
