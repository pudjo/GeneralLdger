<UserControl x:Class="Accounting.Views.UserView.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting.Views.UserView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Baris 0: Toolbar Atas -->
            <RowDefinition Height="*"/>
            <!-- Baris 1: Area Splitter -->
            <RowDefinition Height="Auto"/>
            <!-- Baris 2: Toolbar Bawah -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Tambah User Baru" 
                    Command="{Binding ShowDetailCommand}" 
                    CommandParameter="{x:Null}" 

                     Background="#007ACC" Foreground="White" BorderThickness="0"/>
          
            
        </StackPanel>
        <DataGrid x:Name="dgUser"  Style="{StaticResource DataGridStandar}" 
          AutoGenerateColumns="False" 
          CanUserAddRows="False" ItemsSource="{Binding DaftarUser}"
      SelectionMode="Single"
      GridLinesVisibility="Horizontal" Margin="0,42,0,5" Grid.RowSpan="2">

            <DataGrid.Columns>
                <!-- Kolom ID (Invisible / Tersembunyi) -->
                <DataGridTextColumn Header="ID" 
                        Binding="{Binding ID}" 
                         />

                <!-- 2. Kolom Tombol Detail -->
                <DataGridTemplateColumn Header="Aksi" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Detail" 
                           Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"                                     CommandParameter="{Binding}"
                                    />

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 3. Kolom Nama -->
                <DataGridTextColumn Header="Nama User" 
                        Binding="{Binding Name}" 
                        Width="*" />
                <DataGridTextColumn Header="Email" 
        Binding="{Binding Email}" 
        Width="*" />
                <DataGridTextColumn Header="No HP" 
Binding="{Binding HandPhoneNumber}" 
Width="*" />
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
