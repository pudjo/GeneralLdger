<Window x:Class="Accounting.Views.UserView.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Accounting.Views.UserView"
        xmlns:customcontrols="clr-namespace:Accounting.Views"
        mc:Ignorable="d"

        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
                Title="Login" Height="360" Width="420" WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Border BorderBrush="#DDD" BorderThickness="1" Padding="16" Background="White" CornerRadius="4">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="320" >
                <TextBlock Text="Login" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" HorizontalAlignment="Center"/>

                <TextBlock Text="User ID" FontSize="12" Margin="0,4,0,2"/>
                <TextBox x:Name="txtUser"
                         Text="{Binding LoginDto.UserID, UpdateSourceTrigger=PropertyChanged}"
                         Height="28" />

                <TextBlock Text="Password" FontSize="12" Margin="0,12,0,2"/>
                <PasswordBox x:Name="pwdBox"
                             Height="28"
                             PasswordChanged="PasswordBox_PasswordChanged" />

                <TextBlock Text="Tahun" FontSize="12" Margin="0,12,0,2"/>
                <ComboBox x:Name="cmbYear"
                          SelectedValue="{Binding SelectedYear, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          SelectedValuePath="Content"
                          Height="28">
                    <ComboBoxItem Content="2025"/>
                    <ComboBoxItem Content="2026"/>
                    <ComboBoxItem Content="2027"/>
                    <ComboBoxItem Content="2028"/>
                    <ComboBoxItem Content="2029"/>
                    <ComboBoxItem Content="2030"/>
                </ComboBox>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#D7596D" Margin="0,10,0,0" TextWrapping="Wrap"/>

                <Button Content="Log In"
                        Command="{Binding LoginCommand}"
                        Height="36"
                        Margin="0,14,0,0"
                        HorizontalAlignment="Center"
                        Width="120"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
