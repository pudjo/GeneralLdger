<UserControl x:Class="Accounting.Views.GeneralLedger.LaporanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting.Views.GeneralLedger"
             xmlns:controls="clr-namespace:Accounting.Views.Controls"

             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.Resources>
            <Style x:Key="DarkHeaderStyle" TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="BurlyWood"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="8,10"/>
                <Setter Property="BorderBrush" Value="#34495E"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
            </Style>

            <Style x:Key="BlackButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#1A1A1A"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="14,6"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#444444"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#000000"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="200"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F9F9F9" CornerRadius="4" Padding="10" Margin="0,0,0,15" BorderBrush="#E0E0E0" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Tanggal " FontWeight="Bold" Margin="0,0,0,8" Foreground="#2C3E50"/>

                    <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,15">
                        <!-- DateRangePicker Control -->
                        <DatePicker 
    SelectedDate="{Binding Tanggal, Mode=TwoWay}"
    Width="150"
    Height="32"
    VerticalAlignment="Center"
    Padding="8,5"
    FontSize="12"/>


                        <!-- Tombol Tampilkan -->
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <Button 
            Content="Tampilkan Laporan" 
            Command="{Binding LoadGeneralLedgerCommand}" 
            Padding="15,8" 
            Background="#007ACC" 
            Foreground="White" 
            BorderThickness="0"
            FontWeight="Bold"/>
         
                            
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1" />
        </Grid>

        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#E0E0E0"/>

        <Grid Grid.Column="2" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#34495E" Padding="12" CornerRadius="4" Margin="0,0,0,10">
                <DockPanel LastChildFill="False">
                    <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                        <TextBlock Text="NERACA/BALANCE SHEET" Foreground="White" FontSize="14" FontWeight="Bold"/>
         
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="Tampilkan Data" 
                            Style="{StaticResource BlackButtonStyle}" 
                            Command="{Binding LoadGeneralLedgerCommand}" Background="#2ECC71"
                            Margin="0,0,6,0"/>

                        <Button Content="Cetak" 
        Command="{Binding CetakPdfCommand}" 
        Background="DeepSkyBlue" Foreground="White" 
BorderThickness="0"
FontWeight="Bold" Margin="0,0,6,0"/>

                        <Button Content="Export Excel" 
                            Style="{StaticResource BlackButtonStyle}" 
                            Command="{Binding ExportExcell}" Background="#2ECC71"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <DataGrid Grid.Row="1" 
          ItemsSource="{Binding ListLaporan}" 
          AutoGenerateColumns="False" 
          IsReadOnly="True"
          SelectionMode="Single"
          GridLinesVisibility="Horizontal"
          HeadersVisibility="Column"
          BorderBrush="#DDDDDD"
          Background="White"
          RowBackground="#FFFFFF"
          AlternatingRowBackground="#FBFBFB"
          ColumnHeaderStyle="{StaticResource DarkHeaderStyle}">

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="2,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Root}" Value="1">
                                <Setter Property="FontWeight" Value="Bold"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding Root}" Value="2">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Tag" Value="40,0,0,0"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Root}" Value="3">
                                <Setter Property="FontWeight" Value="Regular"/>
                                <Setter Property="Tag" Value="60,0,0,0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Kode Akun" Binding="{Binding AccountCode}" Width="90"/>

                    <DataGridTextColumn Header="Nama Akun" Binding="{Binding AccountName}" Width="400">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TextStyleWrapping}">
                                <Setter Property="Margin" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Tag}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <DataGridTextColumn Header="Periode Ini" Binding="{Binding CurrentAmount, StringFormat={}{0:N2}}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,5,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Periode Lalu" Binding="{Binding BeforeAmount, StringFormat={}{0:N2}}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,5,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>

</UserControl>
