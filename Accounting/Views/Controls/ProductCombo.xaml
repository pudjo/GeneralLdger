<UserControl x:Class="Accounting.Views.Controls.ProductCombo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="220">
    <Grid x:Name="LayoutRoot">

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="36"/>
            <ColumnDefinition Width="36"/>
        </Grid.ColumnDefinitions>
        

        <ComboBox   x:Name="cmbProduct"
                                      Grid.Column="0"
                  ItemsSource="{Binding Products}"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding SelectedProductId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  IsEditable="True"
                  IsTextSearchEnabled="False"
                  StaysOpenOnEdit="True"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Center"/>
        <!-- Search dialog button -->
        <Button Grid.Column="1" Width="28" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center"
                Click="BtnJenis_Click" ToolTip="Search products">
            <TextBlock Text="..." FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Button>


    </Grid>
</UserControl>
