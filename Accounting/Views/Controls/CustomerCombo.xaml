<UserControl x:Class="Accounting.Views.Controls.CustomerCombo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Accounting.Views.Controls"
             mc:Ignorable="d" 
             Height="32" MinWidth="220">
    <Grid x:Name="LayoutRoot">
        <ComboBox ItemsSource="{Binding Customers}" x:Name="InnerComboBox"
                  DisplayMemberPath="Value"
                  SelectedValuePath="Key"
                  SelectedValue="{Binding SelectedCustomerId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  IsEditable="False"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Center"
                  SelectionChanged="InnerComboBox_SelectionChanged"/>

        
    </Grid>
</UserControl>

